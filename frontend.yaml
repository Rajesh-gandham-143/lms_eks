apiVersion: apps/v1
kind: Deployment
metadata:
  name: lms-fe
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lms-fe
  template:
    metadata:
      labels:
        app: lms-fe
    spec:
      containers:
        - name: frontend-container
          image: ravi2krishna/lms-fe
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: BACKEND_URL
              value: "http://a22c391e5d9aa4af7821e24ae205ed68-1651720342.ap-southeast-2.elb.amazonaws.com:32516"  # Backend service URL

---
apiVersion: v1
kind: Service
metadata:
  name: lms-fe-service
spec:
  type: LoadBalancer
  ports:
    - port: 80              # Port exposed by the service
  selector:
    app: lms-fe